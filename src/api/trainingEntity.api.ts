/**
 * DEFERRED — not part of the active runtime API layer.
 *
 * Canonical today:
 * - Entities: `src/lib/api/entities.ts` (`paths.entities.root`)
 * - Invitations: `src/lib/api/invitations.ts` (`paths.entities.acceptInvitation`)
 *
 * Do not import this file until a deliberate migration moves callers here.
 */

export {};
